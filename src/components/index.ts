/**
 * Library entry point for the iCanCall component set.
 *
 * Re-exports both tiers so consumers — and the design-system sync — have a
 * single module to resolve.
 */
export * from "./ui";
export * from "./domain";
